

export const initialState = {
    // subState: subInitialState,
}

export const mainReducer = (initialState: unknown, action: unknown) => {
  return {
    // subReducer: subReducer(initialState.subState, action),
  };
};
